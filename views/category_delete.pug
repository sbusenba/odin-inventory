extends layout

block content
  h1 Category: #{category.name} delete?
    for item in itemsincategory 
      p= item.name+ " : item in category, please remove prior to delete"
    else
      form(method='POST' action='')
        button(type='submit') Confirm Delete
    
